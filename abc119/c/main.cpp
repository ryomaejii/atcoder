// 開始: 10:25
// 終了: 10:45 溶けなかった。

#include <bits/stdc++.h>
using namespace std;

#define rep(i, n) for (int i = 0; (i) < (n); ++(i))

int N, A, B, C;
vector<int> l;

int main()
{
  cin >> N >> A >> B >> C;
  rep(i, N)
  {
    int li;
    cin >> li;
    l.push_back(li);
  }

  int answer = 0;

  // Aから順に

  cout << answer << endl;
}